package pages;

import core.PageObjectBase;
import io.appium.java_client.AppiumDriver;
import org.openqa.selenium.WebDriver;

public class GithubIO extends PageObjectBase<WebDriver> {

    public GithubIO(WebDriver driver) {
        super(driver);
    }
}
